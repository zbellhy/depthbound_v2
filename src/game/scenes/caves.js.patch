--- a/src/game/scenes/caves.js
+++ b/src/game/scenes/caves.js
@@
   update(dt){
-    const st = this.sm.state;
+    const st = this.sm.state;
+    // UI-active/frame gate: ignore scene input while a UI is open or just closed
+    if (st.uiActive || (st.uiBlockFrames>0)){
+      if (st.uiBlockFrames>0) st.uiBlockFrames--;
+      return;
+    }
     const input = st.input;
